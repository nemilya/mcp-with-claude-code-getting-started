[project]
name = "my-library-mcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["fastmcp>=0.1.0"]

[project.scripts]
my-library-mcp = "my_library.server:main"

[tool.uv]
managed = true
